## 1. 스토리지 파일 생성
### cmd를 열고 shared 폴더 만들기
<img width="1081" height="713" alt="스크린샷 2026-04-09 161304" src="https://github.com/user-attachments/assets/bf0344e5-7f9b-43f7-ae52-f3eb5c160e45" />

### shared에서 스토리지 파일 생성
```bash
-- 호스트(윈도우) 에서 수행
vboxmanage createmedium --filename CRS1.vdi --size 1024  --format VDI --variant Fixed
vboxmanage createmedium --filename CRS2.vdi --size 1024  --format VDI --variant Fixed
vboxmanage createmedium --filename CRS3.vdi --size 1024  --format VDI --variant Fixed
vboxmanage createmedium --filename DATA.vdi --size 30720 --format VDI --variant Fixed
vboxmanage createmedium --filename FRA.vdi  --size 20480 --format VDI --variant Fixed
```

<img width="1021" height="383" alt="스크린샷 2026-04-09 161506" src="https://github.com/user-attachments/assets/375c4138-38bd-44ec-a512-7328bcc552d7" />


- 만약 다음과 같이 에러가 발생할 시 시스템 환경 변수에 Virtualbox 폴더 경로를 추가한 뒤에 디스크를 생성해야 된다.

```
  # 환경변수 경로 설정
  SET PATH=%PATH%;C:\Program Files\Oracle\VirtualBox
```

- 경로 설정하고 재실행

<img width="1057" height="418" alt="경로 설정하고 재실행" src="https://github.com/user-attachments/assets/15ee9d79-3220-42a5-babd-f6f9a7f57db2" />




### 디스크를 공유 가능 모드로 변경

```
vboxmanage modifymedium CRS1.vdi --type shareable
vboxmanage modifymedium CRS2.vdi --type shareable
vboxmanage modifymedium CRS3.vdi --type shareable
vboxmanage modifymedium DATA.vdi --type shareable
vboxmanage modifymedium FRA.vdi  --type shareable
```

※만약 생성된 디스크를 지우고 싶으면 다음의 명령어를 입력한다.

```
vboxmanage closemedium disk CRS1.vdi  --delete
vboxmanage closemedium disk CRS2.vdi  --delete
vboxmanage closemedium disk CRS3.vdi  --delete
vboxmanage closemedium disk DATA.vdi --delete
vboxmanage closemedium disk FRA.vdi  --delete
```

## 2. VM에 디스크 추가

### Not Atteched의 디스크를 하나씩 선택하여 추가
<img width="569" height="636" alt="Not Atteched의 디스크를 하나씩 선택하여 추가" src="https://github.com/user-attachments/assets/d661c45c-a99e-48d2-9cbc-f35aa61a1ca5" />

- 순서가 다르지 않게 주의
<img width="735" height="478" alt="순서" src="https://github.com/user-attachments/assets/596ebc11-51c0-40e0-abf7-df89a2586793" />

## 3. ASM 환경 구성
### ASMLib를 초기화하고 구성

```
oracleasm configure -i    # oracle, dba, y, y 입력
oracleasm init
oracleasm configure
```

<img width="425" height="92" alt="init 에러" src="https://github.com/user-attachments/assets/49529750-4746-4dba-a86c-3bdb5fc760e1" />

- 다음과 같이 init에서 에러나면 아래 코드 실행

```
1. 문제 확인

# 현재 커널 확인
uname -r
# 결과: 5.15.0-200.131.27.el8uek.x86_64 (UEK 커널)

# kmoe-oracleasm 패키지를 설치
yum install kmod-oracleasm

# ASMlib 패키지 확인 (3개의 결과 나와야 한다)
rpm -qa | grep oracleasm

# ASMlib 초기화 시도 
oracleasm init
# 에러: Loading module "oracleasm": failed

2. 사용 가능한 커널 확인

 설치된 커널 목록 확인
grubby --info=ALL | grep -E "^kernel=|^title="

3. 기본 커널을 RHCK(4.18.0)로 변경

# 커널 경로로 직접 설정
grubby --set-default=/boot/vmlinuz-4.18.0-513.5.1.el8_9.x86_64

# 인덱스로 설정
grubby --set-default-index=1

# 설정 확인
grubby --default-kernel
grubby --default-index

4. GRUB 설정 재생성

# GRUB 기본값 확인
cat /etc/default/grub | grep GRUB_DEFAULT

# BIOS 부팅용 GRUB 재생성
grub2-mkconfig -o /boot/grub2/grub.cfg

# UEFI 부팅용 GRUB 재생성
grub2-mkconfig -o /boot/efi/EFI/redhat/grub.cfg

5. 재부팅
reboot

6. 재부팅 후 확인 및 ASMlib 초기화

# 커널 확인 (4.18.0으로 변경됨)
uname -r

# ASMlib 초기화 
oracleasm init

# ASMlib 설정
oracleasm configure -i
 # oracle, dba, y, y 입력

# 상태 확인
oracleasm status

# 구성확인
oracleasm configure
```

- 아래처럼 뜨면 성공
<img width="619" height="476" alt="성공" src="https://github.com/user-attachments/assets/9c37f5bb-c27c-450e-9793-3a45b91edccc" />

